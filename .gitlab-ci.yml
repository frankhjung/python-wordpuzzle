# This file is a template, and might need editing before it works on your project.
# Official language image. Look for the different tagged releases at:
# https://hub.docker.com/r/library/python/tags/
image: python:latest

# Change pip's cache directory to be inside the project directory since we can
# only cache local items.
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

# Pip's cache doesn't store the python packages
# https://pip.pypa.io/en/stable/reference/pip_install/#caching
#
# If you want to also cache the installed packages, you have to install
# them in a virtualenv and cache it as well.
cache:
  paths:
    - .cache/pip
    - venv/

stages:
  - build
  - test
  - run

before_script:
  - python -V
  - pip install virtualenv
  - virtualenv venv
  - source venv/bin/activate
  - pip install -r requirements.txt

check:
  stage: build
  script:
    - yapf --style google --diff --parallel main.py wordpuzzle/wordpuzzle.py tests/testwordpuzzle.py
    - pylint main wordpuzzle.wordpuzzle tests.testwordpuzzle

test:
  stage: test
  script:
    - pytest -v tests/test*.py

run:
  stage: run
  script:
    - python -m main -h
    - python -m main --version
    - python -m main -s 4 -m c -l adevcrsoi -d dictionary/british
